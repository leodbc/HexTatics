# Edge Functions

- validate-map.js: valida e sanitiza payload de mapa no publish.
- verify-solvability.js: placeholder para verificação assíncrona de solvabilidade.

Observação: para produção, implemente endpoints de /v1/maps e telemetria de runs, além de integrar service key no servidor.
